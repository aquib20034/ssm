import React, { FunctionComponent } from "react";
import styles from "./index.module.scss";
import Link from "next/link";

type Props = {
  routes: {
    title: string;
    path: string;
  }[];
};

const BlogCardS2: FunctionComponent<Props> = ({ routes }) => {
  return (
    <div>
    </div>
  );
};

export default BlogCardS2;
